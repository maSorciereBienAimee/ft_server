
pour exec script mariadb : mysql start et mairadb < script.sql sur la meme ligne
mettre le init.sh chmod 755
dans le container : pour mettre port et en meme temps navigateur, mix des deux commandes
placer un index dans le /localhost
php7.3 start

nginx > stock les sites
config nginx : modif default et relink avec enabled 
placer les dossier des sites dans le chemin indique rdans le default (root) et configurer les dossier inc.conf


